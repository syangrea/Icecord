<% if logged_in? %>
    <script type="text/javascript">
        window.currentUser = <%= render(
            "api/users/user.json.jbuilder",
             user: current_user
        ).html_safe %>

        window.servers = <%= render(
            "api/users/servers.json.jbuilder",
            user: current_user
        ).html_safe %>
    </script>
<% end %>

<script>
    window.discordLogo = "<%= image_url("discord_logo.png") %>"
</script>
<main id="root"></main>